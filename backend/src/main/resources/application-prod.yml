spring:
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: ALWAYS
      cleanup-cron: 0 0 1 * * *
  datasource:
    hikari:
      max-lifetime: 300000 # 5 minutes
  autoconfigure:
    exclude: [ ]
